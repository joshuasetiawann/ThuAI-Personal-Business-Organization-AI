# Repo Cleanup Report

**Date:** 2026-06-03 13:36 UTC

## Outcome: cleanup could NOT be performed in this environment
Every delete attempt in the mounted project folder returns **`Operation not permitted`** (the sandbox can create/write files but cannot unlink/delete them here). **Nothing was deleted.** The items below must be cleaned on the founder's own machine (use the script in the next section).

## Junk identified (safe to delete on the founder's machine)
Directories:
- `.pytest_cache/`
- `frontend/dist/` (stale old build; safe — regenerated by `npm run build`)
- `backend/**/__pycache__/` (all of them: backend root, adapters, adapters/optional, agents, alembic, alembic/versions, api, api/routes, core, db, services, tests, tools)

Files:
- `.DS_Store` (root, `backend/`, `frontend/`, `frontend/dist/`, `data/`)
- `backend/__pycache__/*.pyc` (`main`, `config`, `__init__`)
- Session-generated root ZIPs: `post-demo-settings-memory-delta.zip`, `final-reset-handoff-delta.zip`

Not present (nothing to remove): `.mypy_cache`, `.ruff_cache`, `backend/dist`, `coverage/`, `.coverage`, `*.log`.

## Safe cleanup script created
`scripts/safe_repo_cleanup.sh` — a conservative, founder-runnable cleaner.

- **Dry run (default, deletes nothing — just prints what it would remove):**
  ```bash
  bash scripts/safe_repo_cleanup.sh
  ```
- **Apply (actually delete the safe junk):**
  ```bash
  bash scripts/safe_repo_cleanup.sh --apply
  ```

Behavior / guardrails:
- Refuses to run unless executed from the repo root (checks `docker-compose.yml` + `frontend/package.json` exist).
- Removes only: `__pycache__`, `.pytest_cache`, `.mypy_cache`, `.ruff_cache`, `coverage/`, `frontend/dist`, `.DS_Store`, `*.pyc`, `.coverage`, and **root-level** generated ZIPs matching `*delta*.zip`, `*handoff*.zip`, `*cleanup*.zip`, `*post-demo*.zip` (printed before deletion).
- Never descends into `frontend/node_modules` or `.git`.
- Never deletes `.env`, `docker-compose.yml`, `node_modules`, source, docs, lockfiles/requirements/migrations, uploaded data, or any volume/model file.

## Intentionally NOT deleted (and never delete without explicit founder approval)
- Docker volumes; **Ollama model volume**; Postgres data; Redis data; n8n data.
- `.env`, `docker-compose.yml`, source files (`backend/`, `frontend/src/`), `docs/`.
- `package.json` / `package-lock.json` / `requirements.txt` / Alembic migrations.
- `frontend/node_modules` (not touched).
- Uploaded user documents / knowledge data.

## Build status
✅ `tsc --noEmit` clean; `vite build` clean (verified to a fresh output dir; default `frontend/dist` stays OS-locked in this sandbox — delete it locally before `npm run build`).

## Safety reminder
> Never delete Docker volumes, the Ollama model volume, the Postgres volume, `.env`, or source files without explicit founder approval. Never run `docker compose down -v`, `docker system prune --volumes`, or `docker volume rm`. Never pull/reinstall/reset Ollama models unless the founder explicitly asks.
